fastapi==0.111.0
uvicorn==0.30.1
chromadb==0.5.0
openai==1.30.1
pydantic==2.7.2
numpy<2.0.0      # chromadb 0.5.0 usa np.float_ (removido na NumPy 2.0) — fixar em 1.x
python-dotenv    # load_dotenv() no app.py lê o .env (PM2 não injeta sozinho)
