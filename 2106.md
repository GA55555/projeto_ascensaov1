# Handoff 21/06/2026 — Auditoria de Design & Limpeza dos Pop-ups (aba Mundo)

> Documento de contexto para iniciar a **próxima sessão**. Lê primeiro a `ARQUITETURA.md` (lei dourada) e os memorandos de longo prazo (em especial [[project-modais-utilitarios]], [[project-board-texto-seguranca]], [[project-zoom-double-scaling]], [[project-sem-postgres-validacao-estatica]]). **Nada abaixo foi testado contra Postgres/browser reais** — só validação estática (`node -c`, pareamento def↔uso por grep, boot 200). Confirmar ao vivo antes de assumir que funciona.

## Escopo da sessão
Foco principal: **revisão de design de TODOS os pop-ups usados em `controle_mundo.html`** contra as Regras 2.5 (Design System / proibição de inline estético / coesão de cores) e 2.6 (contraste de estados), seguida de uma **limpeza fatiada (Slices 1–6)**. Antes disso, na mesma sessão, houve correções de UX no Tabuleiro (registradas em memória) — ver "Outras mudanças da sessão".

Arquivos tocados: `public/css/global_ui.css`, `public/controle_mundo.html`, `public/js/controle_mundo.js`, `validators/mundoValidator.js`.

---

## 1. Auditoria de design (diagnóstico)
Pop-ups inventariados: modais estáticos do HTML (forja, evento, vínculo, núcleos, núcleos-evento, mover-núcleo, automação, sessão), modais injetados via JS (sinapses, contrato, mapa-sinapses), popovers do board (`montarPopover`: nó/zona/símbolo/texto/linha/ligação-local) e `modalConfirmacao.js`.

**Conformes:** `.modal`/`.modal-box` (glass + sombra em camadas + radius 8px), internos dos popovers do board (tokens, radius 4px, `--transicao-suave`, hover com inversão de contraste — Regra 2.6).

**Não conformes encontrados (todos corrigidos nas Slices, salvo o que está em "pendências"):**
- `.board-popover`: `box-shadow: var(--sombra-profunda)` era **só cor → declaração inválida → sem sombra**; e usava `--bg-card` opaco **sem `backdrop-filter`** (Regra 2.5 exige glass em painel flutuante). **CORRIGIDO** (agora glass igual `.modal-box`).
- `.board-card`: mesmo bug `box-shadow: var(--sombra)` (só cor) → sem sombra em repouso. **CORRIGIDO** (`0 2px 8px var(--sombra)`).
- **61 `style=""` inline estéticos** no HTML + dezenas no JS (Regra 2.5 proíbe).
- 1 cor hardcoded `rgba(0,0,0,0.2)` (automação).

---

## 2. O que foi feito (Slices 1–6)

**Slice 1 — utilitários de modal + curas de sombra.** Criou `.modal-box-md`, `.modal-acoes`, `.modal-box > h3:first-child` (reset de margem); curou sombra do `.board-card`; migrou larguras/rodapés/títulos dos 8 modais estáticos. Inlines HTML 61→42.

**Slice 2 — wrappers dos modais JS.** `.modal-sinapses-box` (480px) e `.modal-contrato-box` (440px); removeu o inline do wrapper externo.

**Slice 3 — generalização de cabeçalho.** Renomeou `.board-modal-head`/`.board-modal-titulo` → **`.modal-head`/`.modal-titulo`** (genéricos) e aplicou nos 2 modais do board + 3 injetados (sinapses/contrato/mapa). **Atenção: os nomes `board-modal-*` não existem mais.**

**Slice 4 — one-offs estáticos.** `.modal-lista` (×3), `.modal-vinculo-lista` (×3), `.modal-linha` (+`input[type=text]{flex:1}`), `.select-multi`, `.modal-detalhes` (+`summary`). Adicionou `overflow-wrap: break-word` na `textarea` base. Inlines HTML 42→30. **Modais estáticos ficaram sem inline exceto 3 (automação).**

**Slice 5 — fechamento dos estáticos (decisão do usuário).** Token novo **`--bg-afundado: rgba(0,0,0,0.2)`** no `:root`; `.modal-label-forte`, `.modal-automacao-campos`. **Modais estáticos: 100% sem inline.** Inlines HTML 30→27 (os 27 são FORA de pop-up).

**Slice 6 — corpos dos modais JS.** `.campo-label`, `.input-full`, `.sinapse-lista` (+`info-block-vazio`), `.sinapse-form`, `.sinapse-col-ent/-tipo/-max`, `.modal-head-botoes`. Corpos de `modal-sinapses` e `modal-contrato`: 100% sem inline estático. **Preservada de propósito** a barra de pressão `style="width:${pct}%; background:${cor}"` em `corpoContratoHTML` (data-driven — Regra 2.5 permite largura % por dados).

### Catálogo reutilizável criado (em `global_ui.css`)
`.modal-box-md`, `.modal-box>h3:first-child`, `.modal-acoes`, `.modal-sinapses-box`, `.modal-contrato-box`, `.modal-head`, `.modal-titulo`, `.modal-head-botoes`, `.modal-lista`, `.modal-vinculo-lista`, `.modal-linha`, `.select-multi`, `.modal-detalhes`(+`summary`), `.modal-label-forte`, `.modal-automacao-campos`, `.campo-label`, `.input-full`, `.sinapse-lista`, `.sinapse-form`, `.sinapse-col-ent/-tipo/-max`. Token: `--bg-afundado`. **Regra de ouro: ao criar/editar modal, REUSAR estas classes — nunca re-inlinar estética.** (Detalhe em [[project-modais-utilitarios]].)

### Balanço
Todos os pop-ups (estáticos + JS + popovers do board) livres de inline estético e cor hardcoded, com glass/sombras corretos. Cor hardcoded 1→0; sombras inválidas 2→0.

---

## 3. Tarefas A/B — CONCLUÍDAS (sessão seguinte, mesmo dia 21/06)

> Catálogo completo das classes criadas em [[project-cards-toolbars-utilitarios]] (memória). Validação **só estática** (`node -c` OK, pareamento def↔uso por grep, sem Postgres/browser) — smoke visual ao vivo pendente.

**Tarefa A — Inlines FORA de pop-up no HTML (27 → 0). FEITO.** Extraídas 14 classes em `global_ui.css` e migrados todos os `style=` estáticos: `.page-container`, `.cronica-nome`, `.secao-toolbar`, `.toolbar-acoes`, `.input-busca`, `.secao-nota`, `.grid-sessoes`, `.sessao-detalhes`, `.detalhe-titulo`, `.detalhe-meta`, `.detalhe-secao-titulo`(+`.com-espaco`), `.detalhe-resumo`, `.detalhe-lista`. Os `display:none/grid` de `#sessao-detalhes`/`#grid-sessoes` continuam como toggle funcional do JS (a classe dá o default, o JS sobrescreve — Regra 2.5 permite). Unificações mínimas reportadas: header de detalhes `mb 20→15px`, toolbars com `gap:10px`, `<ol>` desfechos `14→13px`.

**Tarefa B — Inlines estéticos no JS (71 → 11). FEITO.** Migrados os 3 card-renderers (evento/automação/sessão), o renderer de detalhe (listas), lista de núcleos, flag-rows e empty-states para ~30 classes (scaffold de card compartilhado `.card-col`/`.card-topo*`/`.card-rodape*`/`.card-titulo`/`.truncate`/`.col-full`/`.btn-mini`/`.nota-mini`/`.texto-erro` + específicas de evento/automação/sessão). Os **11 `style=` restantes são data-driven/layout legítimo e NÃO foram tocados** (barras `width:%`, board `left/top/transform`, `-webkit-mask-image`, `stroke` SVG — Regra 2.5 permite).
- **Cores hardcoded curadas (0 restantes no JS):** barra de pool `#2ecc71/#f1c40f/#e74c3c` → classes por threshold (`.barra-fill` verde / `.barra-fill--aviso` / `.barra-alerta`); `rgba(0,0,0,0.2)`→`--bg-afundado`; bordas de footer `rgba(255,255,255,0.05)`→`--borda-glass`; chip de gatilho `rgba(255,255,255,0.03)`→`--bg-afundado`.
- **Bônus arquitetural:** hover do card de sessão migrado de `onmouseover/onmouseout` inline → CSS `:hover` (`.card-clicavel`, Regra 2.6); órfã pré-existente `.flex-shrink-0` (usada, nunca definida) agora definida.

**Fundo do board tokenizado (fatia extra, a pedido do Narrador). FEITO.** `aplicarCamera` não tem mais `canvas.style.backgroundImage` com cor hardcoded: o padrão grid/dots virou classe CSS (`.board-canvas--grid`/`--dots`) com cor em token (`--board-grid-cor` `rgba(255,255,255,0.05)`, `--board-dots-cor` `rgba(255,255,255,0.12)`). O JS faz `classList.toggle(tipo)` e mantém só `background-size/-position` inline (data-driven/câmera, anti-Moiré intacto). `none` = ambas as classes off (idêntico ao antigo `'none'`). Tema 16-bit não sobrescreve os tokens → padrão visual idêntico. **0 cores hardcoded em `controle_mundo.js`.**

### Balanço final
HTML: 27→0 inlines. JS: 71→11 (os 11 são exceções data-driven). Cores hardcoded no JS: 0. Classes novas 100% pareadas (def↔uso, sem órfãs nem lixo). `node -c` OK.

### Smoke test ao vivo PENDENTE (validar antes de assumir OK)
Tudo abaixo só foi validado estaticamente:
- **Aba Mundo/Eventos/Automações/Sessões:** abrir cada aba; conferir cards (evento: barra de pool muda de cor verde→amarelo→vermelho por pct, badge de núcleos truncada; automação: tabela de parâmetros na caixa afundada, toggle armado/desarmado; sessão: hover destacando borda, clamp de 4 linhas no resumo, status colorido).
- **Painel de detalhe da sessão:** abrir uma sessão (títulos, listas de entidades/eventos/desfechos/saves, botões mini).
- **Toolbars:** filtros, busca (largura), botões de núcleos.
- **Tabuleiro:** trocar fundo no select (Pontilhado/Grade/Liso); confirmar que o padrão aparece, segue a câmera no pan/zoom e o anti-Moiré não chia; testar no tema pixel-16bit (zoom 1.5).

---

## 4. Outras mudanças da sessão (Tabuleiro — também SÓ validadas estaticamente)
Contexto caso surjam regressões; todas pendentes de teste ao vivo:
- **Caixa de texto do board:** `width: fit-content` + `overflow-wrap/word-break` (anti-vazamento); estilo **"Placa Coesa"** nos fundos `semi`/`denso`; campo **`t.align`** (left/center/right, classes `.board-text-center/-right`, novos textos nascem `center`); **schema Zod `align` adicionado** em `texts[]` (`dadosBoardSchema`). Fix da **falsa indentação inicial**: `<span>` cola no `<div>`/`</div>` no template (whitespace + `pre-wrap` virava texto). Ver [[project-board-texto-seguranca]].
- **Símbolos (props) do board:** escala passou a ser via **tamanho do box** (`width/height = 48×scale`), não `transform: scale` — corrige hit-box/“pular ao arrastar” e melhora âncora das linhas. Rotação fica no transform. (`PROP_LADO_BASE=48`, `.board-prop` 48×48 base, `.board-prop-icone` 100%.)
- **Posicionamento de popover do board:** `posicionarPopover` agora é **zoom-aware** (divide coords por `getComputedStyle(:root).zoom`), espelhando `posicionarTooltipHover` — fim do "menu foge" (double-scaling do `:root{zoom:1.33}`). Ver [[project-zoom-double-scaling]].
- **Fundos do tabuleiro + anti-Moiré:** select `board-fundo-select` (dots/grid/none), `boardState.fundo`, `mudarFundoBoard`, laço anti-chiado em `aplicarCamera`; chave `fundo` no `dadosBoardSchema`.
- **Ícones de exclusão flutuantes removidos:** botões `.board-*-remover` apagados; exclusão migrou para os popovers; `fecharPopover()` nos `remover*Board` (anti-órfão).

## 5. Como retomar / validar ao vivo
1. Reiniciar o servidor (sem nodemon): `node server.js` (porta 3000) + hard-refresh.
2. **Smoke test dos modais:** abrir forja/evento/vínculo/núcleos/mover/automação/sessão + sinapses/contrato/mapa e os popovers do board (nó/zona/símbolo/texto/linha). Conferir: larguras, rodapés de ação, listas roláveis, caixa "afundada" da automação, glass+sombra dos popovers, formulário do sinapses (colunas Entidade/Tipo/Máx., barra de pressão), placa/alinhamento do texto, drag de símbolos colado ao cursor, posicionamento do popover perto/longe da origem (e no tema pixel-16bit, zoom 1.5).
3. Ambiente **sem Postgres** (porta 5432 fechada) — endpoints que tocam o banco dão 401/500 aqui; o boot só confirma rota registrada. Ver [[project-sem-postgres-validacao-estatica]].

## 6. Ponteiros
- Lei dourada: `ARQUITETURA.md` (raiz).
- Memória: `MEMORY.md` no diretório de memória — em especial `project-modais-utilitarios`, `project-board-texto-seguranca`, `project-zoom-double-scaling`, `project-board-accent-cascade`, `project-estado-arquitetural`, `feedback_arquitetura_md`, `feedback-verificar-premissas-prompt`.
- Handoffs anteriores: `1806.md`, `1706.md` (raiz).

---

# SESSÃO 21/06 (continuação) — FASE 14: Células de Núcleo + Diplomacia

> Tudo validado **só estaticamente** (`node -c`, boot=rotas registradas, pareamento def↔uso por grep). **Sem Postgres/browser** nesta sessão → smoke ao vivo pendente. Memórias detalhadas: [[project-fase14-celulas-nucleo]] e [[project-fase14-diplomacia]].

## A. Células de Núcleo (Smart Containers) — FEITO
Moldura `.board-celula` (em `boardState.celulas`) que representa um núcleo, importa seus membros e os move em bando. Arquivos: `controle_mundo.js/.html`, `global_ui.css`, `mundoValidator.js`.
- **Modelo correto (premissa do prompt corrigida):** núcleos são `entidade_nucleos` (`nucleosCache.entidade`), **não** `world_nodes` tipo 'nucleo'. node↔núcleo é direto via `world_node.nucleo_id` (no `boardNodesCache`); membros no board cruzam via `boardNodeInfo(id)`. Não existe `nodeHTML` (card é inline no `renderBoard`).
- **F1 — Import + render + arrasto em bando:** `celulas` no `dadosBoardSchema` + `boardVazio()` + carga defensiva; botão "Importar Núcleo"; `importarCelulaBoard` (auto-importa membros + grade); `celulaHTML` (Banner de Facção: faixa de cabeçalho na cor da facção via `.board-cor-*`); colapso (`is-minimizada` + membros `is-membro-oculto`); `ativarArrastoCelulas` (snapshot 1×, delta leve). `.board-celula` na exclusão do `ativarPanZoom`.
- **F2 — Popover de opções:** `abrirEditorCelula` (cor, reorganizar, desimportar=mantém cards, remover=cards+moldura).
- **F3 — Resize + slider:** handle `.board-celula-resize` (re-flui a grade ao vivo) + slider "Largura" no popover (`setCelulaLargura`); `reflowMembrosCelula`/`colsDaCelula`/`alturaConteudoCelula` (DRY). Mín. 220×140.
- **F4 — Re-sync de membros:** passivo (`carregarMesaGuerra` sempre `renderBoard()` com cache fresco → grupo/ocultação refletem mudança de núcleo na aba Mundo) + ativo (botão "Sincronizar membros" → re-fetch nós, importa faltantes, reorganiza). Não remove cards.
- **Bug "arrasto preso" (CURADO, board-wide):** faltava tratar `pointercancel` (só o Pan tratava). Adicionado em TODOS os drags (card, zona+resize, `arrastarPorPonteiro`, célula+resize) + `user-select:none` no `.board-celula`. **Regra: todo drag novo registra pointerup E pointercancel.**

## B. Diplomacia entre Núcleos — FEITO (persistência ATIVA)
Relações Aliados/Inimigos/Neutros entre núcleos. Controle **global na aba Mundo** (fonte da verdade); **Tabuleiro só reflete** (auto-draw read-only entre Células).
- **Banco:** tabela **`nucleo_diplomacia` criada hoje pelo DBA** (`sql/nucleo_diplomacia.sql`; FKs CASCADE, CHECK no status). Persistência ativa.
- **Backend:** `GET/PUT /cronicas/:id/diplomacia` (mundoRoutes); `salvarDiplomacia` = **bulk replace em transação** com **anti-IDOR 3.3.1** (núcleos têm de ser da crônica); `salvarDiplomaciaSchema` (Zod, A≠B); `MundoApi.getDiplomacia` (tolerante)/`salvarDiplomacia`.
- **Frontend Mundo:** botão "Diplomacia" + `#modal-diplomacia` (lista **agrupada por status**); `diplomaciaCache` + `abrirModalDiplomacia/adicionar/remover/renderizarLista/persistir`. Adicionar par existente troca o status; dedupe em qualquer ordem.
- **Board:** `data-nucleo` adicionado ao `celulaHTML`; bloco de macro-links em `desenharLinhasBoard` (linha fantasma derivada SÓ do cache, não toca `boardState`); estilo `.board-line-diplomacia` (média + halo, `opacity:0.85`, cor por status); cache carrega em `carregarMesaGuerra`.

## C. Smoke test ao vivo PENDENTE (Fase 14 inteira)
1. Reiniciar `node server.js` + hard-refresh.
2. **Células:** Importar Núcleo → grade; minimizar/expandir (membros somem/voltam); **arrasto em bando** (não trava ao soltar); resize (handle + slider Largura) re-fluindo colunas; popover (cor/reorganizar/desimportar/remover); mover NPC de núcleo na aba Mundo → voltar ao Tabuleiro (passivo) e "Sincronizar membros" (ativo); Salvar + recarregar; tema pixel-16bit.
3. **Diplomacia:** abrir modal → criar/trocar/remover relações (agrupadas por status) → com 2+ Células no board, ver as linhas coloridas + halo seguindo o arrasto → **recarregar e confirmar persistência** (tabela já existe).
4. Conferir anti-IDOR: PUT /diplomacia com núcleo de outra crônica → 404.
