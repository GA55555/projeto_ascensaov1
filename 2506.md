# Relatório de Sessão — 25/06/2026 · Projeto Oráculo (RAG)

> **Plano-mestre:** [`oraculo.md`](./oraculo.md) (leia-o sempre antes de mexer no Oráculo).
> **Lei dourada:** `ARQUITETURA.md`. Ambiente atual sem Postgres/browser → tudo validado
> **estaticamente** + alguns testes locais do serviço Python; **smoke ao vivo de ponta a ponta PENDENTE**.

---

## 0. 🧭 PRÓXIMA SESSÃO — guia de retomada

**Onde estamos:** F1 (microsserviço) ✅ · F2-lado-Python ✅ (provado localmente) · F2-lado-Node **em
andamento** (conector criado, ganchos ainda NÃO aplicados). F3/F4/F5 não começaram.

**Faça nesta ordem:**
1. **Aplicar os 3 ganchos** no `controllers/mundoController.js` (`criarNode`/`editarNode`/`deletarNode`)
   — os snippets corretos (com as variáveis REAIS) estão na §4 deste doc. **Adicionar** no topo:
   `const oraculoClient = require('../services/oraculoClient');`
2. **Aplicar a mudança do `/remover`** no `app.py` (apagar por `where={cronica_id, entidade_id}`; tirar
   `tipo` do `RemoverRequest`) — §4.
3. **Validar variáveis** de `criarEvento`/`deletarEvento` (mundoController) e `cenasController` antes de
   replicar os ganchos (mesmo padrão; NÃO confiar nas variáveis "de cabeça" — checar o código real).
4. **`.env` em produção** (não vêm pelo git): Node (`ORACULO_URL`, `ORACULO_SHARED_SECRET`); Python
   (`OPENAI_EMBEDDINGS_KEY`, o MESMO `ORACULO_SHARED_SECRET`, `ORACULO_PORT`). Depois **`pm2 restart`** dos dois.
5. **Smoke ao vivo (maior débito):** criar um NPC no Tabuleiro → `pm2 logs oraculo` mostra o upsert real
   (embedding OpenAI); apagar → `/remover`.

**Bloqueado por terceiros:** o **DBA** precisa aplicar a **DDL** (§6 do `oraculo.md`): `cronicas.oraculo_ativo`
+ `usuarios.oraculo_gen_key/url/model`. Sem isso: o gate é só o global por `ORACULO_URL` e o BYOK não liga.

**Depois disso:** F3 (Big Bang) → F4 (preencher `/consultar` = RAG) → F5 (front opt-in + chat).

---

## 1. O que foi decidido (tudo travado — §7 do `oraculo.md`)
- **Embeddings:** OpenAI `text-embedding-3-small`, **pagos pelo PROJETO** (chave no `.env`, não BYOK).
- **Geração (BYOK, por utilizador):** padrão **DeepSeek** (`base_url=api.deepseek.com`, `model=deepseek-chat`),
  trocável p/ OpenAI (`gpt-4o-mini`). Chave **cifrada** em `usuarios` (AES, `ORACULO_ENC_KEY`), write-only.
  **Só a consulta (F4) usa a chave do Narrador**; o `/upsert` usa a chave do projeto.
- **Stack:** SDK `openai` (faz geração E embeddings). Sem Anthropic, sem Voyage.
- **Ambiente:** **venv + pm2** (sem Docker — o AMD A6 já roda 6 contêineres).
- **Escopo:** Mundo (NPC/Facção/Local), Eventos, **Sessões e Automações** (textos longos → **chunking**).
- **Isolamento (Regra 3.3.1):** metadata `cronica_id` em tudo + `query/delete where={cronica_id}` — provado.

## 2. F1 — Microsserviço (✅ FEITO)
- **Local:** `~/dev/projeto_ascensao/oraculo_service/` — **FORA** do repo Node (`projeto_ascensaov1/`).
  Contém `app.py` (FastAPI), `requirements.txt`, `venv/`, e (a criar) `.env`.
- **`app.py`:** `127.0.0.1` + header de segredo (`X-Oraculo-Secret`, Regra 6.4); ChromaDB `PersistentClient`
  em `chroma_data/` (SSD, Regra 4.1); `load_dotenv()`; coleção com `embedding_function=None` (nunca carrega
  ONNX); cliente OpenAI com `OPENAI_EMBEDDINGS_KEY`. Rotas: `/health`, `/upsert`, `/remover`, `/consultar`(stub).
- **RSS em produção: ~6,6MB** (pm2) — muito abaixo da meta de 150MB. ✅
- **🔴 Bloqueador resolvido — NumPy:** `chromadb 0.5.0` quebra com **NumPy 2.x** (`np.float_` removido).
  **Fixado `numpy<2` (1.26.4)** no `requirements.txt`. *(Sempre instalar via `requirements.txt`.)*
- **Bug menor pendente:** a linha `chromadb.config.Settings(anonymized_telemetry=False)` **não aplica nada**
  (objeto solto); p/ silenciar a telemetria de verdade, passar `settings=` no `PersistentClient`. Cosmético.

## 3. F2 — lado Python (✅ FEITO, provado localmente)
- **`/upsert`:** embeddings via OpenAI `text-embedding-3-small` → `colecao.upsert(ids=[f"{tipo}:{entidade_id}"],
  embeddings=[vetor], metadatas=[{cronica_id,tipo,entidade_id}], documents=[texto])`. Idempotente. 500 se faltar a chave.
- **`/remover`:** apaga por `where={cronica_id}` (será ajustado p/ `{cronica_id, entidade_id}` — §4).
- **Provado no venv:** upsert+query+delete OK; **consulta com crônica errada → vazio** (anti-IDOR confirmado).

## 4. F2 — lado Node (🟡 EM ANDAMENTO)
- **✅ Criado `projeto_ascensaov1/services/oraculoClient.js`** (Regra 2.4): `enviarParaOraculo(acao, dados)`
  fire-and-forget (sem `await`), header de segredo, timeout 2s, e **gate**: se `ORACULO_URL`/`ORACULO_SHARED_SECRET`
  não estão no `.env`, vira **no-op** (deploy do Node é seguro mesmo sem o Python pronto). Node v22 ✓ (fetch global).
- **❌ Ganchos NÃO aplicados.** O guia da outra IA tinha **variáveis FALSAS** (validação de premissa):
  é `cronicaId` (não `cronica_id`); **não existem `descricao`/`dados_ficha`**; o `deletarNode` **não tem `tipo`**.
  **Snippets CORRETOS (colar em `mundoController.js`):**
  - `criarNode` (após o INSERT, antes do `res.status(201)`):
    ```js
    const node = novoNode.rows[0];
    oraculoClient.enviarParaOraculo('upsert', { cronica_id: cronicaId, tipo: node.tipo, entidade_id: node.id,
        texto: `Nome: ${node.nome}\nTipo: ${node.tipo}` });
    ```
  - `editarNode` (antes do `res.json`):
    ```js
    const node = result.rows[0];
    oraculoClient.enviarParaOraculo('upsert', { cronica_id: cronicaId, tipo: node.tipo, entidade_id: node.id,
        texto: `Nome: ${node.nome}\nTipo: ${node.tipo}\nDados: ${JSON.stringify(node.dados || {})}` });
    ```
  - `deletarNode` (antes do `res.json`, SEM tipo):
    ```js
    oraculoClient.enviarParaOraculo('remover', { cronica_id: cronicaId, entidade_id: nodeId });
    ```
- **❌ Mudança do `/remover` no `app.py` NÃO aplicada** (necessária porque o delete não tem `tipo`):
  ```python
  class RemoverRequest(BaseModel):
      cronica_id: str
      entidade_id: str
  # delete por metadata (e ainda apaga os chunks da entidade):
  colecao_oraculo.delete(where={"$and": [{"cronica_id": req.cronica_id}, {"entidade_id": req.entidade_id}]})
  ```

## 5. Deploy no servidor físico (analisado — git NÃO basta)
`git pull` traz **só o Node** (oraculoClient + ganchos). **NÃO** traz: o `oraculo_service/` (está FORA do repo →
versionar num repo próprio ou rsync); o `venv/` (recriar + `pip install -r requirements.txt`); os `.env`
(no `.gitignore` — criar à mão); o `chroma_data/` (vazio → rodar F3); a DDL (DBA). E **reiniciar** Node+Python
(pm2 não recarrega). Bom: o **gate** torna o deploy do Node seguro mesmo sem o Python no ar. Branch certa
(lembrar do episódio sandbox×main); Node ≥18 no prod.

## 6. Débitos / riscos
- **Smoke ao vivo de ponta a ponta** (maior débito): criar/editar/apagar → ver upsert/remover no Python; F4 ainda stub.
- **DDL do DBA** pendente → sem ela, BYOK e opt-in per-crônica não funcionam.
- **Custo de API** (embeddings + geração) — medir; chunking já planejado para não estourar contexto.
- Telemetria do chromadb (cosmético) e `oraculo_service` sem repo git próprio (recomendado criar).

## 7. Mapa de arquivos
- **Plano-mestre:** `projeto_ascensaov1/oraculo.md`
- **Serviço Python (fora do repo):** `~/dev/projeto_ascensao/oraculo_service/` → `app.py`, `requirements.txt`, `venv/`
- **Conector Node:** `projeto_ascensaov1/services/oraculoClient.js`
- **Ganchos a aplicar:** `controllers/mundoController.js` (79 criar, 94 editar, 135 deletar, 454 criarEvento,
  482 deletarEvento) e `controllers/cenasController.js` (39/56/78).
- **Este relatório:** `projeto_ascensaov1/2506.md`
